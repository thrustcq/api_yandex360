info:
  name: Снять запрет на авторизацию во внешних OAuth-сервисах
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base_url}}/security/v1/org/{{orgId}}/oauth_access_restriction"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
