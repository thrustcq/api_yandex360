info:
  name: Просмотреть список участников группы
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/directory/v2/org/{{orgId}}/groups/{{groupId}}/members"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
