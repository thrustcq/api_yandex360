info:
  name: Посмотреть список подразделений
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/directory/v1/org/{{orgId}}/departments?perPage=4"
  params:
    - name: orderBy
      value: name
      type: query
      disabled: true
    - name: page
      value: "1"
      type: query
      disabled: true
    - name: parentId
      value: ""
      type: query
      disabled: true
    - name: perPage
      value: "4"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
