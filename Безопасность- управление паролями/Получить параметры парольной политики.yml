info:
  name: Получить параметры парольной политики
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/security/v1/org/{{orgId}}/domain_passwords"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
