info:
  name: Получить время жизни cookie сессии
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/security/v1/org/{{orgId}}/domain_sessions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
