info:
  name: Просмотреть список приложений
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/security/v1/org/{{orgId}}/service_applications"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
