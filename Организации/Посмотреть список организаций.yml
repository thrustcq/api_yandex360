info:
  name: Посмотреть список организаций
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/directory/v1/org"
  params:
    - name: pageSize
      value: "1"
      type: query
      disabled: true
    - name: pageToken
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
