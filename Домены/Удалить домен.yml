info:
  name: Удалить домен
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base_url}}/directory/v1/org/{{orgId}}/domains/{{domain}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
