info:
  name: Получить статус подключения домена
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/directory/v1/org/{{orgId}}/domains/{{domain}}/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
