info:
  name: Включить DKIM подпись для домена
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/v1/org/{{orgId}}/domains/{{domain}}/dkim/enable"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
