info:
  name: Получить статус DKIM для домена
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/directory/v1/org/{{orgId}}/domains/{{domain}}/dkim"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
