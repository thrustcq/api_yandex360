info:
  name: Посмотреть список общих ящиков
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/admin/v1/org/{{orgId}}/mailboxes/shared?perPage=35"
  params:
    - name: perPage
      value: "35"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
