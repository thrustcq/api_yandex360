info:
  name: Выключить для ящика возможность делегирования
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/admin/v1/org/{{orgId}}/mailboxes/delegated/{{resourceId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
