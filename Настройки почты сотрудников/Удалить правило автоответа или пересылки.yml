info:
  name: Удалить правило автоответа или пересылки
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/admin/v1/org/{{orgId}}/mail/users/{{userId}}/settings/user_rules/{{ruleId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
