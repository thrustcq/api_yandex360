info:
  name: Сбросить телефон для 2FA
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/directory/v1/org/{{orgId}}/users/{{userId}}/2fa"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
