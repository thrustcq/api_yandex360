info:
  name: Просмотреть информацию
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/directory/v1/org/{{orgId}}/users/{{userId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
