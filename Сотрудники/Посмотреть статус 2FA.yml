info:
  name: Посмотреть статус 2FA
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/directory/v1/org/{{orgId}}/users/{{userId}}/2fa"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
