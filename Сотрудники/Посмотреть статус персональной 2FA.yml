info:
  name: Посмотреть статус персональной 2FA
  type: http
  seq: 12

http:
  method: GET
  url: "{{base_url}}/directory/v1/org/{{orgId}}/users/{{userId}}/domain_2fa"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
