info:
  name: Загрузить портрет
  type: http
  seq: 9

http:
  method: PUT
  url: "{{base_url}}/directory/v1/org/{{orgId}}/users/{{userId}}/avatar"
  body:
    type: file
    data:
      - filePath: C:\Users\ThrustCQ\Documents\bruno\Yandex 360\Сотрудники\pic.jpg
        contentType: image/jpeg
        selected: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
