info:
  name: Посмотреть список
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/directory/v1/org/{{orgId}}/users"
  params:
    - name: page
      value: "2"
      type: query
      disabled: true
    - name: perPage
      value: "14"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
