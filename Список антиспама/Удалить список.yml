info:
  name: Удалить список
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base_url}}/admin/v1/org/{{orgId}}/mail/antispam/allowlist/ips"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
