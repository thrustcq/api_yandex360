info:
  name: Удалить контакт
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/directory/v1/org/{{orgId}}/external_contacts/{{contactId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
