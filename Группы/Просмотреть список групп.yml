info:
  name: Просмотреть список групп
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/directory/v1/org/{{orgId}}/groups"
  params:
    - name: page
      value: "9"
      type: query
      disabled: true
    - name: perPage
      value: "1"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
