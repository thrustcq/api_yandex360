info:
  name: Удалить участника из группы
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{base_url}}/directory/v1/org/{{orgId}}/groups/{{groupId}}/members/{{type}}/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
