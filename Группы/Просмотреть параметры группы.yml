info:
  name: Просмотреть параметры группы
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/directory/v1/org/{{orgId}}/groups/{{groupId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
