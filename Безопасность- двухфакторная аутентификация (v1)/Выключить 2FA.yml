info:
  name: Выключить 2FA
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base_url}}/security/v1/org/{{orgId}}/domain_2fa"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
